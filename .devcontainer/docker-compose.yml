services:
  app:
    build:
      context: ./node
      dockerfile: Dockerfile
    container_name: spec-board
    volumes:
      - ..:/workspace:cached
    ports:
      - "4000:4000"
      - "4001:4001"
    command: sleep infinity
